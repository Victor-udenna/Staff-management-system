import styled from 'styled-components'

const NetPaymentStyle = styled.div`
  margin-top: 40px;

  .netpayment-header {
    color: #000;
    font-weight: 600;
    font-size: 17px;
  }
`

export default NetPaymentStyle
