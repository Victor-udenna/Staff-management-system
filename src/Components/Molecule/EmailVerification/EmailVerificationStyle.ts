import styled from "styled-components";

const EmailVerificationStyle = styled.div`
.email__status{
    display: flex;
    align-items: center;
    gap: 6px;
    font-size: 13px;
  }

    .send__verification__button{
        color: red;
        border: 0px;
        background-color: white;
        color: orange;
        font-weight: 600;
        cursor: pointer;
    }

`

export default EmailVerificationStyle;